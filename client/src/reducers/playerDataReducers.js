const initialState = {
  stats: [],
  teams: '',
  countries: '',
  dataType: '',
}

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
