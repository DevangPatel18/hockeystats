const initialState = {
  yearStart: '20182019',
  yearEnd: '20182019',
  reportName: 'skatersummary',
  playoffs: false,
  isAggregate: false,
}

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
